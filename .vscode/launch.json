{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "EntryFrame Run",
      "request": "launch",
      "mainClass": "view.EntryFrame",
      "cwd": "${workspaceFolder}",
      "classPaths": [
        "${workspaceFolder}/bin",
        "${workspaceFolder}/lib/*"
      ],
      "preLaunchTask": "Compile LibrarySystem"
    }
  ]
}